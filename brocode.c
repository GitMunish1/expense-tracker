#include <stdio.h>
int main(){
    int length ,breath;
    printf("what is the length fo rect.\n");
    scanf("%d",&length);
    printf("what is the breath of rect.\n ");
    scanf("%d",&breath);
    printf("the area of your rectangel is %d",length*breath);
    return 0;
}